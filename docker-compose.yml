services:
  octopus:
    build:
      context: .
      args:
        VERSION: dev
    image: octopus-local:dev
    ports:
      - "8080:8080"
    volumes:
      - "./data:/app/data"
    container_name: octopus
    restart: unless-stopped
