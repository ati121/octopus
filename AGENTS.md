# 仓库协作约定

## 开始工作前

1. 必须先完整阅读根目录的 `MAINTENANCE.md`，以它作为项目当前状态和发布上下文的事实来源。
2. 检查 `git status -sb`、当前分支和远端映射，确认没有覆盖用户未提交的修改。
3. 本仓库默认分支为 `dev`，新的主远端为私有仓库 `ati121/octopus`。

## 语言与提交

- 与维护有关的说明、交接记录和提交主题统一使用简体中文。
- 提交信息可保留 Conventional Commits 前缀，但冒号后的主题必须使用中文，例如：`fix: 修复缓存命中率显示`。
- 不重写上游历史提交来翻译旧英文信息；只保证本仓库后续新增提交使用中文。

## 上下文维护

- 每次完成用户可见功能、仓库迁移、版本发布、镜像发布或工作流调整后，都要同步更新 `MAINTENANCE.md`。
- 记录已经完成的事实、验证结果、当前限制和明确的下一步，不记录 Token、密码或其他密钥。
- 私有仓库的更新检查依赖运行环境变量 `OCTOPUS_GITHUB_PAT`，严禁把 Token 写入代码、文档示例值或 Git 历史。

## 验证与发布

- 根据改动范围运行 Go 测试、前端 ESLint 和生产构建，并在 `MAINTENANCE.md` 更新最近验证结果。
- 发布前确认目标仓库、分支、版本号和镜像标签，不能把旧仓库地址带入新构建。
- 不批量推送旧版本标签到新仓库，避免触发大量历史 Release 工作流。
